{
  "name": "mysterio",
  "version": "1.0.0",
  "private": true,
  "description": "wrapper for t-sheets api",
  "repository": "https://github.com/caseconsulting/mysterio",
  "author": "Case Consulting",
  "scripts": {
    "build": "sam build -t CloudFormation.yaml",
    "deploy": "sam deploy --template-file CloudFormation.yaml",
    "local": "sam build -t CloudFormation.yaml && sam local invoke \"PTOBalancesFunction\" -e events/event.json",
    "test": "jasmine"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  }
}
