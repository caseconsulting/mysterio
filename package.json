{
  "name": "mysterio",
  "version": "1.0.0",
  "private": true,
  "description": "wrapper for t-sheets api",
  "repository": "https://github.com/caseconsulting/mysterio",
  "author": "Case Consulting",
  "scripts": {
    "build": "sam build -t CloudFormation.yaml",
    "deploy": "npm run build && sam deploy --template-file CloudFormation.yaml",
    "local": "npm run build && sam local invoke \"PTOBalancesFunction\" -e events/event.json",
    "test": "jasmine"
  },
  "dependencies": {
    "aws-param-store": "^3.2.0",
    "axios": "^0.18.0",
    "lodash": "^4.17.15",
    "request": "^2.88.2",
    "request-promise": "^4.2.5"
  },
  "devDependencies": {
    "jasmine": "^3.5.0"
  }
}
